title: 3.2. Check JSON request body with JSONPath
description: Stub for checking JSON body with JSONPath.
stub: |
  id: json-object
  conditions:
    method: POST
    jsonPath:
      - "$.phoneNumbers[?(@.type=='iPhone')]"
  response:
    text: JSONPath check successful!