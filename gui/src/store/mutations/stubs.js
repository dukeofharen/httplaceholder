export function storeStubs(state, stubs) {
    state.stubs = stubs;
}

export function storeStubsDownloadString(state, download) {
    state.stubsDownloadString = download;
}

export function storeLastSelectedStub(state, stub) {
    state.lastSelectedStub = stub;
}