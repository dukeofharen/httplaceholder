# HttPlaceholder build utils

A simple .NET project which is used to generate several files during the HttPlaceholder build process. These files are generated:

- swagger.json (for creating REST clients).
- schema.json (JSON schema file for language / validation support in for example VSCode).
- Maybe more in the future.
